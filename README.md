# Artes Nobiles

A simple, responsive, static website. The publishable website is in `dist/`. The Christmas Story appears first, ROSARIUM second, and Flappy Pope third on the homepage. Navigation uses the same order, with the game labeled “Flappy Pope” and linked to `/flappypope/`. At desktop widths of 960px and above, each product’s copy is on the left and its image is on the right; smaller screens keep the stacked category, image, and details layout.

Workflow preference: push completed website updates directly to `master` for immediate production deployment.

Repository: [brantlymillegan/artes-nobiles-website](https://github.com/brantlymillegan/artes-nobiles-website). GitHub Actions deploys the default branch to GitHub Pages. See [GITHUB_PAGES.md](GITHUB_PAGES.md) for the exact DNS records for `artesnobiles.com` and HTTPS. Run `python3 scripts/build_pages.py` to validate and assemble the public release in `_site/`; the workflow publishes only those selected files.

Run a local preview with `python3 -m http.server 4173 --bind 127.0.0.1 --directory dist`, then open http://127.0.0.1:4173.

The Christmas Story reading proof is reference material only and is not included in this project. The supplied cover is included. The ROSARIUM preview uses the latest approved light and dark app videos from “Create Rosarium app animation”. The book's purchase link and release information can be added when available.

The site is deployed on GitHub Pages with artesnobiles.com configured as its custom domain. GoDaddy DNS has been verified to point to GitHub Pages. HTTPS is enabled for the apex and www domains, and HTTP/www requests redirect to https://artesnobiles.com/.

The header uses the latest full-name SVG seals supplied in “Design company logo”, from `output/artes-nobiles-logos/animation-full-growth/`: black in light mode and white in dark mode, following the System / Light / Dark selection. All four SVGs and the supplied motion helper are copied unchanged. The artwork spells “Artes Nobiles” and includes the approved rounded joined vine base. The two-second animation plays once on page load, then settles into the matching static logo. Mouse hover, a tap/click, or keyboard focus replays it with a 240 ms crossfade from the static version. Repeated triggers during playback are ignored. Both theme variants stay synchronized, and reduced-motion preferences keep the logo still. Static images remain the fallback if JavaScript or animation loading fails. The footer remains text. The favicon uses the same static black seal, with the curved name removed, a solid white disk inside the outer circle, and transparency outside. Its SVG retains the original logo paths and both borders; the ICO fallback provides 16, 32, and 48 px versions.

Link previews use the page title “Artes Nobiles” and description “Sacred art, modern media” in the standard description, Open Graph, and X/Twitter Card metadata. `dist/og.png` is a 1200×630 opaque PNG with the exact white static full-name logo centered on the dark background `#252a32`; no additional text is drawn into the image. Image dimensions, MIME type, alternative text, and absolute HTTPS URLs are included in the initial HTML so preview crawlers do not need JavaScript.

Brand rules for this site:
- Do not invent company or product slogans or mottos.
- Render the ROSARIUM wordmark with its official Noto Serif variable font: weight 720 and 0.18em letter spacing. Pair each visible title and navigation wordmark with the approved app icon on its left, using 22% rounded corners.
- In every visible book title, set “The” smaller and italic, vertically centered to the left of “Christmas Story.”
- Center “The” against the main letters’ bodies, excluding the long tail of the “y.” The shared title component uses a .744em alignment box and reserves .233em below for the descender, in both the navigation and book heading.
- Match the cover’s exact typefaces: Big Caslon Medium for “Christmas Story” (tracking −0.018em) and Minion Variable Concept Italic for “The” (weight 400, optical size 21.22). The menu and book heading use outlined SVG glyphs from those actual font files, with accessible text, so rendering does not depend on installed fonts. Keep the existing website size ratios and vertical centering. Source measurements and the CoreText outline generator are in `output/christmas-title-fonts/`; no cover font files or book PDF are published.
- Show the app inside the actual Samsung Galaxy S26 device image, keeping the entire status bar within the screen corners.

The Galaxy S26 photograph is the official Samsung base-model Front2 Black asset. The video uses that same photograph around actual Android app footage. Both v8 MP4 files are copied unchanged from `output/rosarium-animation/v8/`: silent, 40 seconds, 60 fps, H.264 with fast start. A 519×1073 frame crops the 600×1200 export’s padding so the phone stays exactly 365px high on mobile, 390px at intermediate widths, and 430–460px on desktop. The matching transparent opening frame provides a still fallback; the unchanged phone mask preserves the exact outline. The video autoplays muted and loops inline, follows the resolved theme while preserving playback position, scales to 104% on hover, and links to rosariumprayer.com in a new tab/window. The preview has no visible playback controls. Reduced-motion preferences pause playback, and hidden tabs pause until visible again. Only the selected theme’s video is loaded.

The System / Light / Dark control matches brantly.com's `app/theme-toggle.tsx` and its control styles. It opens on mouse hover, stays open on click, and closes after selection, Escape, or an outside press. An explicit preference is saved under `artes-nobiles-theme`; System clears that override and follows live OS appearance changes. Both the page and app video follow the resolved theme.

Asset sources:
- Book heading stars: unchanged `assets/ornaments/draft40/bethlehem-star.svg` from the Christmas book project, byte-identical to Canonical 152's packaged `029_bethlehem-star.svg`. The same gold eight-point star and ruby center appear above the landing page's main, section, and sample titles.
- Book page heading colors follow Canonical 152's interior swatches in light mode: Christmas evergreen `#21483D` for titles and Cinnabar `#982F3B` for the small subtitles above them. Dark mode, including the system preference, retains ivory titles and gold subtitles. Those three subtitles use accessible text with exact outlined Minion Variable Concept lettering at the book's Text-10.200 setting and 0.11em tracking; no font files are published.
- Book section dividers: the Christmas book project's `assets/ornaments/draft111/scripture-bottom-fleuron.svg`, byte-identical to Canonical 152's packaged bottom-of-page ornament. `dist/assets/book/bottom-flourish.svg` preserves the original 160 × 36 vector and its gold, evergreen, and ruby palette. The compact ornament appears above the video and between the video, reader, and sample spreads. The former long divider asset remains available for cached stylesheets.
- ROSARIUM app icon: unchanged approved 512×512 Play Store icon from `../../Rosary app/play-store/assets/play-icon-512x512.png` (in the user’s Documents folder), identified by that project’s `play-store/LOGO-REFRESH.md`. The export script derives this image from the Android launcher layer, including its outer padding. Every website instance uses a centered 150% image inside a fixed-size wrapper with 22% rounded corners, reproducing Android’s visible 72dp viewport within the full 108dp adaptive layer. This removes the extra launcher-layer padding without adding a border or altering the cross artwork. See [Android’s AdaptiveIconDrawable sizing](https://developer.android.com/reference/android/graphics/drawable/AdaptiveIconDrawable).
- App Store badge: unchanged English SVG from [Apple](https://developer.apple.com/assets/elements/badges/download-on-the-app-store.svg), downloaded September 12, 2026. Displayed as a disabled control with reduced opacity and “Coming soon” beneath it until the iOS listing is available.
- Google Play badge: unchanged English SVG from Google’s [Partner Marketing Hub](https://partnermarketinghub.withgoogle.com/brands/google-play/google-play/lockups-icons-badges/), downloaded September 12, 2026. Displayed at 48px high with at least 12px surrounding clear space, linked directly to ROSARIUM’s listing.
- ROSARIUM video and posters: version 8 from `output/rosarium-animation/v8/`, recorded September 18, 2026 from freshly built Android 0.1.3 (version code 7, source revision `812b43c`). Both use the September 18 app updates, Friday’s Sorrowful Mysteries, the expanded Creed scroll down and back up, the Continue-button pointer circle, Saved Place dismissal before browsing tabs, and Prayers scrolling back to the top before Novenas. Actual emulator captures use 1080×2340, 420 logical dpi, font scale 1.0, and the original Samsung frame. Public files are `rosarium-demo-light-v8.mp4`, `rosarium-demo-dark-v8.mp4`, `rosarium-video-poster-light-v8.png`, and `rosarium-video-poster-dark-v8.png`. Previous v6 and v7 video URLs and the shared phone mask remain available for cached pages. Private build and capture provenance stays under `output/` and is excluded from publication.
- ROSARIUM font: https://rosariumprayer.com/fonts/noto-serif-variable.ttf
- Samsung Galaxy S26: https://images.samsung.com/is/image/samsung/p6pim/us/s2602/gallery/us-galaxy-s26-s942-sm-s942uzkexaa-550995407?fmt=png-alpha&wid=2000&hei=2000

The product navigation stays at the top once scrolled past. A compact, theme-matched logo fades in on its left and replays the same animation on mouse hover or activation. Activating it scrolls to the top in 360 ms without replaying the main logo; keyboard focus moves to the header. User scrolling interrupts the return, and reduced motion returns immediately. On smaller screens the compact logo is always visible beside the menu button. On desktop it is removed from the tab order while hidden, section anchors clear the sticky bar, and reduced motion preserves static logos.

Both company logos scale to 104% on mouse hover and ease back to normal on pointer leave, independently of their SVG animation. This follows the existing product hover timing and respects reduced motion.

The homepage presents the latest narrow Draft 152 animation from `output/book-animation/mobile-v3/`, published under `assets/book/film-152-mobile-v3/`. Both silent H.264 films and their lossless PNG posters are 1584 × 1224 (22:17); the films run 61.54 seconds at 24 fps with fast start. The cover and blank front leaf open together so the tour begins directly on the printed pages, with no blank green opening spread. Both modes use the page backgrounds and explicit sRGB transfer, matching the wide films. The animation sits to the right of the book details on desktop and above them on smaller screens, preserving its original proportions. It autoplays muted on a loop as it approaches the viewport, follows the selected light/dark theme, and scales gently on mouse hover. Clicking, tapping, or keyboard-activating the animation opens the dedicated book page in the same tab. Native player controls remain hidden, and reduced motion starts with the still poster. The homepage no longer loads the reader or shows its prompts and controls; its title and Learn More button also link to the dedicated book page.

The dedicated book page retains the complete interactive book. Clicking its cover opens once to the first printed page (the gift inscription), without advancing to the title spread. Desktop shows the front endpaper with the cover artwork credit beside page 1; narrow layouts show page 1 on its own. Clicking the left/right page or using the arrow controls turns backward/forward; Escape, Close book, or a click outside closes it to the front cover. Narrow layouts display one page at a time and preserve native pinch zoom. The left third of a single page turns backward and the right two-thirds turns forward; two-page spreads retain the left-page/back and right-page/forward split. The instruction text follows changes in orientation. The reader includes accessible page text, supports keyboard arrows, honors reduced motion, preloads nearby pages rather than downloading the whole book at page load, and allows retry after a page-image loading failure.

Reader content is the complete Draft 152 (Canonical 152) edition, rendered from the final “Reference - Reading Order.pdf” in the Draft 152 print package: all 52 printed interior pages, six endpaper faces, and both covers, for 60 WebP images in their bound reading order. The closed cover uses the same new front-cover image as the reader. All images are 1600 × 1236; text pages use lossless encoding. Every page was rendered from this edition, including its revised artwork, Scripture, captions, picture index, and cover. No PDF, native InDesign files, print package, or private provenance is published. Rendering records and visual checks are in `output/book-reader-assets-152/_private/`; the public reading-order manifest is `dist/assets/book/152/manifest.json`. The builder verifies the complete sequence and only publishes explicitly selected assets. Previous editions’ asset URLs remain available for visitors who already have an older reader open.

Page curl and hardcover turns use the locally hosted [StPageFlip](https://github.com/Nodlik/StPageFlip) 2.0.7 module. The npm package was verified against its SHA-512 registry integrity value; its MIT license is included at `dist/assets/vendor/page-flip-LICENSE.txt`. The reader has no runtime CDN or PDF-rendering dependency.

The dedicated book page is at `/thechristmasstory/` (GitHub Pages redirects `/thechristmasstory` there). The homepage navigation links to it. Its compact product navbar stays visible from the top of the page, with the animated, theme-matched Artes Nobiles logo linking home on the left. On both website pages, mobile and tablet widths below 840px use a shared three-line menu that reveals all three product links and the same System / Light / Dark control. The homepage places its appearance controls in this menu on smaller screens; desktop retains the control beside the main logo. The mobile logo is 64px within an 84px bar. The menu icon morphs into a close icon as a full-width panel unfolds from the navbar; the larger product links and appearance controls enter in sequence, and closing folds the panel back up. Reduced motion removes these transitions. System, Light, and Dark are always visible as three labeled choices inside the mobile menu, with the selected preference highlighted. Theme selections preserve focus and keep the menu open. The menu supports Escape, outside presses, link selection, and keyboard focus; desktop retains its inline product links and theme control. The main title retains the cover's exact outlined fonts and centered italic “The.” The page presents the video tour first, followed by a full-width Draft 152 reader that opens in place, and three actual spreads: the Annunciation (8–9), the Birth of Christ (24–25), and Wise Men from the East (30–31). Spreads stack in reading order on small screens, with accessible Scripture transcripts. Purchase controls remain absent until release details are ready.

The book page's light and dark films come from `output/book-animation/v9/`: the approved wide Draft 152 animation with the blank green front endpapers skipped. The blank leaf opens against the inside front cover, directly revealing the printed cover-credit and dedication pages before the title/frontispiece visit. The remaining tour and closing are unchanged. Exports use the landing page's exact light `#f6f4ef` and dark `#252a32` backgrounds, at 1920 × 1200, 24 fps, 61.54 seconds, in silent H.264 with fast start. Explicit sRGB transfer with BT.709 primaries and matrix keeps their colors aligned with the page; lossless PNG posters and the film container use the same background colors. The cover opens immediately, then the camera pauses on the painting facing the interior title page before continuing the tour. The new posters and MP4s are published under `assets/book/film-152-v9/`; earlier versions and URLs remain available. Only the selected theme's video loads, when it approaches the viewport; native player controls stay hidden. A transparent button over the film supports tap, click, or keyboard pause/play without covering the book with player UI. Reduced motion prevents autoplay, while explicit playback remains available. Theme changes preserve playback position and deliberate pauses. The reader uses `data-reader-layout="fixed"` on this page so it opens in place. The release builder validates every HTML route, cross-page anchor, and stylesheet resource against its explicit public-file list.

The homepage Flappy Pope section uses the matched light/dark videos from “Flappy Pope videos” in the Flappy Catholic project (`output/gameplay-showcase/`, September 20, 2026). The original silent, fast-start H.264 exports are published unchanged under `assets/flappy-pope/film-v1/`, with matching WebP posters: 960 × 450, 30 fps, 30 seconds. They show a synchronized automated gameplay replay through ten backgrounds and column designs, changing every three seconds. Only the selected theme’s video loads as the preview approaches the viewport. Playback loops without controls; switching themes preserves the position. Reduced motion, a hidden tab, or scrolling away pauses playback. Matching posters remain visible while loading or if playback fails. The preview, pixel-font title, and Play Now button link to `/flappypope/`. The original pixel pope SVG and Press Start 2P font are copied from that project; the font’s OFL license is included. Earlier still-image URLs remain available for cached pages. The game implementation remains in its own repository and is assembled into the release by GitHub Actions.

Product logos build on the assets from “Animate logos” (`output/project-logo-animations/`). Christmas star revision 4 and ROSARIUM revision 3 remove their initial still periods: both swell immediately with a smooth 320 ms ease-out to a larger first peak, accompanied by early glints and rays. They retain their exact resting artwork and settle smoothly after the later turns/pulses. The star keeps its fuller second sparkle burst. Flappy Pope revision 4 starts immediately with a smooth full backward somersault over 960 ms and a small lift/enlargement. The wings stay in their original pose during the flip, then perform two smooth wingbeats before the four-second animation returns to its exact resting pose. The SVGs are published under `assets/product-logo-animations/`; earlier versions remain available for cached pages. Hovering either the logo or its attached product name plays the four-second animation once, then settles into its resting pose; leaving and hovering again replays it. The prepared SVG stays visible at rest, so hover never fades or swaps image layers. Embedded raster artwork is decoded before the static fallback is replaced. This applies to both navigation menus, all homepage product titles, and the star/main title on the book page. Keyboard focus and touch activation also trigger motion without intercepting links. Reduced motion keeps the original static images, and switching to a hidden tab stops playback. Source fetches are shared, each SVG is isolated in its own shadow root, and padded motion viewBoxes are mapped to preserve the static artwork’s exact size and position. Asset failures retain the static fallback. The existing Artes Nobiles seal animation is unchanged.
